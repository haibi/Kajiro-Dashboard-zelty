# =============================================================================
# TEMPLATE de configuration — copier vers secrets.toml et remplir
# secrets.toml est dans .gitignore. Sur Streamlit Cloud : Settings → Secrets.
# =============================================================================

# --- Zelty API ----------------------------------------------------------------
ZELTY_API_KEY = "PASTE_YOUR_ZELTY_GROUP_KEY_HERE"
ZELTY_AUTH_SCHEME = "Bearer"
ZELTY_BASE_URL = "https://api.zelty.fr"

# --- Supabase (cache + table users) ------------------------------------------
# Format pooler transaction (port 6543) — créer un projet sur supabase.com
SUPABASE_DB_URL = "postgresql://postgres.PROJECT:PASSWORD@aws-0-eu-west-1.pooler.supabase.com:6543/postgres"

# --- Cookie session signing key ----------------------------------------------
# Générer avec : python -c "import secrets;print(secrets.token_urlsafe(48))"
DASHBOARD_PASSWORD = "GENERATE_RANDOM_64_CHAR_STRING_FOR_SIGNING_SESSION_COOKIES"

# --- Claude API (Board AI) — optionnel ---------------------------------------
# ANTHROPIC_API_KEY = "sk-ant-..."

# Les comptes utilisateurs (email, role, restaurants autorisés, mot de passe haché)
# sont gérés dans la table `users` de Supabase via le panneau admin de l'app.
